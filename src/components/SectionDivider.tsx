const SectionDivider = () => (
  <div className="max-w-[1140px] mx-auto px-5">
    <div
      className="h-px w-full"
      style={{
        background: "linear-gradient(90deg, transparent, hsl(var(--gold) / .2) 50%, transparent)",
      }}
    />
  </div>
);

export default SectionDivider;
